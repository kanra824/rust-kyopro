enum Compare {
    EXACT
}

enum JudgeResult {
    AC,
    WA,
    TLE,
    RE,
    MLE,
    CE,
    IE,
}

fn judge(id: i32, solver: fn()) {
}

fn judge_testcase(id: i32, serial: i32, solver: fn()) {
    unimplemented!();
    // input 読み込み
    // output 読み込み
    // 子プロセスを起動して solver を実行
    // output と result を比較 compare(output, result);

}

fn compare(output: String, result: String) {
    
}